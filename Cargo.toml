[package]
name = "ggez-hitbox"
version = "0.1.0"
edition = "2024"

[dependencies]
ggez = { version = "~0.10.0", default-features = false, features = ["gamepad"] }
# rand = "~0.10.2"

[profile.dev]
opt-level = 1

[profile.release]
lto = true
opt-level = 2
strip = true
debug = false
panic = "abort"
